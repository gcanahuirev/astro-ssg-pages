{
  "name": "astro-ssg-pages",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=24.18.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "astro check",
    "biome:check": "biome check . --write",
    "cf:preview": "astro build && wrangler pages dev",
    "cf:deploy": "astro build && wrangler pages deploy"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.1.4",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@biomejs/biome": "^2.5.5",
    "typescript": "^6.0.3",
    "wrangler": "^4.114.0"
  }
}